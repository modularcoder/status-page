{
  "name": "ui-starter-scss",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build  && echo statuspage.gh.am > ./dist/CNAME  && gh-pages -d dist --repo git@github.com:modularcoder/status-page.git --branch gh-pages"
  },
  "dependencies": {
    "vue": "^3.2.47"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.1.0",
    "gh-pages": "^5.0.0",
    "sass": "^1.62.0",
    "vite": "^4.2.0"
  }
}
